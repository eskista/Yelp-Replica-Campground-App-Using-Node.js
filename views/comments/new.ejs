<%- include("../partials/header") %>
<div class="container">
	<div class="container">
		<h1 style="text-align: center">Add New Comment to <%= campground.name %></h1>
			<div style="width: 30%; margin:30px auto">
				<form action="/campgrounds/<%= campground._id %>/comments" method="POST">
					<div class="form-group">
<!-- under name here, the reason we are doing "comment[text]" than just text becuase we want the text nested inside of comment when the form is submitted  Inside of my app.js, we will be able to extract comment which has a already a has a text inside of it -->
						<input class="form-control" type="text" name="comment[text]" placeholder="comment">
					</div>
					<div class="form-group">
						<button class="btn btn-lg btn-primary btn-block">Submit!</button>
					</div>
				</form>
				<a href="/campgrounds">Go Back</a>
			</div>
	</div>
</div>
<%- include("../partials/footer") %>